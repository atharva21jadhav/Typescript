// Default values are trailing (Right to left)
function DefaultVals(no1:number, no2:number=10, no3:number=10):void {
    console.log('This is Default Args');
}

DefaultVals(1);
DefaultVals(1,2);
DefaultVals(1,2,3);